{
    "name": "serenite-server",
    "version": "1.0.0",
    "main": "server.js",
    "dependencies": {
        "@vercel/postgres": "^0.5.1",
        "cors": "^2.8.5",
        "cross-env": "^7.0.3",
        "dompurify": "^3.0.6",
        "dotenv": "^16.3.1",
        "express": "^4.18.1",
        "jsonwebtoken": "^9.0.2",
        "nodemailer": "^6.9.8",
        "pg": "^8.11.3",
        "resend": "^3.0.0",
        "uuid": "^9.0.1"
    },
    "scripts": {
        "vercel-build": "ls && ls node_modules && vercel build && vercel deploy --prebuilt",
        "vercel-b": "vercel build && vercel deploy --prebuilt",
        "vercel-push": "vercel --prod",
        "vercel-t": "npm run vercel-b && npm run vercel-push",
        "start": "node server.js",
        "dev": "vercel dev --listen 8000"
    },
    "engines": {
        "node": "18.x"
    }
}
